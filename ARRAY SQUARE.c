#include<stdio.h>
#include<conio.h>
void main()
{
    int i,j,n,a[20];
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);

    }
    for(i=0;i<n;i++)
    {
        printf("%d ",a[i]);
        while(n>0)
        {
            r=n%10;

        }

    }


}
